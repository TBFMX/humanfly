<%= form_for(@suggest_box) do |f| %>
  <% if @suggest_box.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@suggest_box.errors.count, "error") %> prohibited this suggest_box from being saved:</h2>

      <ul>
      <% @suggest_box.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :comment %><br>
    <%= f.text_field :comment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
